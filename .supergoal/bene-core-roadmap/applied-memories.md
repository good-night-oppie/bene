# Applied memories
- accuracy-gate-vs-docs-review — a green proxy gate != verified; ground-truth claims by running. APPLIED: every roadmap leverage was code-ground-truthed (langfuse update_trace bug, probe CLI absence) before becoming a phase.
- handoff-reverify-inflight-suite — re-run the suite yourself, don't trust inherited green. APPLIED: Polish phase re-runs full suite + verifies 0-failed.
- bene-claims-reality — real moat = computed autonomy + hash-locked gate + single-file union; don't overclaim table-stakes. APPLIED: roadmap centers the probe/kill-gate (the verified moat), explicitly de-scopes Temporal-dependent durability.
- feedback-fix-all-before-moving-forward — fix the whole queue top-to-bottom, don't pause to ask ship-or-defer. APPLIED: roadmap runs all phases under one /goal.
