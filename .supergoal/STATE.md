# State: BENE 2.0 Mastermind Redesign + Apple EM Interview Kit

**Status:** COMPLETE
**Current phase:** — (all 10 complete; final audit passed; post-completion claim-audit residue swept 2026-06-13; community-peer reframe + agents-as-users batch shipped 2026-06-14; factory.ai-grammar redesign-v2 shipped 2026-06-15; landing-copy linter scaffold + KILL_LIST + tests + CI wiring shipped 2026-06-15)
**Started:** 2026-06-11
**Last update:** 2026-06-16
**Baseline ref:** 6c55fa3    <!-- fresh git init 2026-06-11 (corrupt .git archived as .git.corrupt-20260611); tiny-commit discipline per ADDENDUM_GIT_PR_DISCIPLINE.md, local-only-by-design. A stale `origin` (github.com/good-night-oppie/bene) exists from before the re-init — its 9-commit history is the 2026-05-30 scaffold and is DISJOINT (no merge-base) from local. Do not push/merge across; the disjoint state is intentional. -->

## Phase progress

| # | Phase | Status | Started | Completed | Notes |
|---|-------|--------|---------|-----------|-------|
| 1 | Mine corpus, audit rivals | complete | 2026-06-11 | 2026-06-11 | 48 cites, 14+13 shortcomings |
| 2 | Mastermind BENE 2.0 design | complete | 2026-06-11 | 2026-06-11 | 10 decisions, 55-row subsumption |
| 3 | Apple interview narrative kit | complete | 2026-06-11 | 2026-06-11 | DEADLINE: Fri 2026-06-12 11:30 PT |
| 4 | Kernel v2 engram substrate | complete | 2026-06-11 | 2026-06-11 | 41 tests, commit 7ad3747 |
| 5 | Trust & falsifiable-eval layer | complete | 2026-06-11 | 2026-06-11 | 26 tests, bba7fc1+7d1dab1 |
| 6 | Evolution engine | complete | 2026-06-11 | 2026-06-11 | 18 tests, b1120c0 |
| 7 | Memory & context OS | complete | 2026-06-11 | 2026-06-11 | 21 tests, eb6fd24 |
| 8 | Harness-engineering layer | complete | 2026-06-11 | 2026-06-11 | 19 tests |
| 9 | Port, DevEx & demo 2.0 | complete | 2026-06-11 | 2026-06-11 | 3acb93d, v0.2.0 |
| 10 | Polish & Harden | complete | 2026-06-11 | 2026-06-11 | claims audit, hardening, kit refresh |

## Engineering check status

- Build: n/a
- Typecheck: n/a
- Lint: ruff clean (2026-06-11)
- Tests: 736 passed, 6 skipped, 0 failed (2026-06-14, this-session run; `uv run python -m pytest tests/ -q`, 106s); +53 new tests for the landing-copy linter in 2026-06-15 (bene-8) bring runnable=789 in-session but 53 are scoped to the linter and not part of bene's core regression invariant — record cont. Drift vs the 741/1/0 recorded 2026-06-13 is purely env (5 postgres-backend tests skip on ConnectionRefusedError; 1 tier-router test skips on missing OPENAI_API_KEY). Was 718 on 2026-06-12 (pre-postgres-engram merge); 699+3 on 2026-06-11 (post-Round-3); pre-Round-3 baseline was 615; +84 tests closed A1b/A2/A4/A5 mechanism gaps. **Invariant: 0 failed; pass/skip counts drift per env.** Latest (2026-06-16, bene-core-3): **976 passed, 8 skipped, 0 failed** in 124s via `uv run --extra a2a python -m pytest tests/ -q` (EXIT=0); +29 vs the 947 recorded for bene-core-2 = the `--extra a2a` suite + the 3 row-63 failure-localize tests; the 8 skips are env-gated (postgres backend + OPENAI_API_KEY); the flaky `test_concurrent_writes_two_threads` did not trip this run. Latest (2026-06-16, bene-core-4): **993 passed, 8 skipped, 0 failed** in 128s (same cmd, EXIT=0); +17 vs 976 = bene-9's docs-round test adds + bene-core-4's +4 (F6 agent_write byte-count ×2, `bene --json diff` ×2).

## Notable events

- 2026-06-16 — bene-core-3 → bene-core-4 baton + docs-accuracy review (Opus 4.8 1M, ultracode; G1 serial-baton 不失忆). Retired predecessor: bene-core-3 FINISHED its in-flight turn before I killed it (committed + pushed its last 2 skills.md punch-list fixes — sk-1 FK-safe save + sk-4 MetaHarnessSearch — as `5a22efd`), then I migrated clients, killed the `bene-core-3` session + sweep daemon (pidfile removed), and spawned the `bene-core-4` daemon (WATCH=`bene-core`, `/tmp/sweep-daemon-bene-core-4.pid` pid 3471082, first sweep clean). **Re-verified the suite myself** (inflight-suite duty, didn't trust inherited green): 989→**993 passed / 8 skipped / 0 failed** (EXIT=0) after my 2 code fixes. **2 bene-core code-lane fixes shipped to origin:** (1) **F6** `cb4ad35` — `bene/mcp/server.py` `agent_write` reported `len(content)` CHARS labeled "bytes" while persisting `content.encode()` UTF-8 bytes → now counts real bytes; +2 first-ever MCP `_dispatch` regression tests. (2) **`bene --json diff`** `536ea28` — the `diff` command was the lone subcommand ignoring the global `--json` flag (no `@click.pass_context`, never called `_json_out`) so `bene --json diff | jq` got Rich tables not JSON, contradicting docs/checkpoints.md:107; wired `@click.pass_context` + `_json_out` (mirrors `checkpoints` cmd); +2 CLI tests; incidental ruff-format of 3 pre-existing over-long autonomy-cmd lines to green `ruff format --check`. **Read-only adversarial docs-accuracy review** (wf_3e18bbec-2a2, 47 agents, every claim RUN vs real CLI/code/schema then verified): **38 confirmed findings** (1 refuted). 2 were code-lane (both fixed above); the other **36 are DOC-lane handed to bene-9** (its editor+deploy lane) via A2A + `/tmp/bene-core-4-docs-punchlist.md` (priority-ordered, exact ground-truthed corrections). Highlights: **docs/meta-harness.md has fabricated MEASURED numbers** (compaction-ladder L0–L10 char/%/quality all wrong; "No level loses signal" false — L10 answers 3/8; five-domains L5 savings overstated) = credibility risk; mcp-integration mh_search/mh_resume Returns fabricated (real = detached-worker `{status,pid,log_path,message}`); cli-reference `bene run --name` wrongly "auto-generated"; 13 use-case demo GIFs don't exist; README "10 tables"→11; broken CORAL anchors ×3. **4 code-vs-doc DECISION items** (`mh write-skill`/paper-benchmarks `-b`/`--dataset-corpus`/`mh knowledge <arg>`): recommended doc-trim now, thin-CLI as optional bene-core follow-up. Accuracy gate = VERDICT=ACCEPT (install + hard-numbers, the class the gate covers). **bene-9 is IDLE with the 36-item doc queue pending** — flagged to harness orchestrator for nudge/reassign (doc edits + deploy belong to bene-9's lane, not split to bene-core). Run remains COMPLETE; bene-core lane green + done.
- 2026-06-16 — Docs punch-list round — 4 batches SHIPPED (bene-9 editor, bene-core-3 reviewer/accuracy_gate; user /goal now covers ALL docs, supersedes the prior STAND DOWN). Followed the 0.30 landing pass; bene-core-3 handed a 56-finding docs punch-list (`/tmp/bene-9-docs-punchlist.md`) + an accuracy_gate that kept surfacing install-404 residuals. Every fix ground-truthed against the real CLI/code, EN+ZH parity, docs HTML regenerated, mirrored (bene-site + agentdex-cli origin+deploy-target) + Koyeb each batch. **Commits:** `9a5ef77` (batch1), `53be471` (batch2), `cbeba26` (batch3), `184e1ec` (batch4). **Fixed:** site-wide install-404 → 0 (architecture `bene[fuse]`, t10 `uv add bene`, design-exploration copy button — honest `pip install bene`/coming-to-PyPI everywhere, no 404ing copy buttons); **cli-reference PATH preamble** (fresh-checkout first command was `command not found`); schema version 1→4 + tables 8→11; architecture busy_timeout 5000→30000 (×3) + wal_autocheckpoint + max_retries 3→1 + compaction(ARCH-4) + ARCH-5(10+→11+); mcp F1(18→37)/F2(mh_resume `benchmark` ×2 payloads — was KeyError)/F3(fictional v0.5.2 de-versioned)/F4(3 unusable benchmarks dropped)/F5(fabricated 10-32x removed); README `uv run`; memory mem-1(spawn-first FK; spawn→str); checkpoints CP-1(checkpoint() takes no metadata→manager)/CP-2(db.gc→db.blobs.gc); schema json_object_length→json_each; use-cases fraud_detection→script + COV_DROP_LIMIT→BENE_COV_DROP_LIMIT + CORAL attribution; codegen-1/2/3/4 (stale-status: runtime/temporal primitives landed + submit_side_effect closed; dead `.cursor/`/private-worktree refs → real `bene/runtime/` refs — no .cursor paths in customer docs). All live-verified (LIVE EN babel-compiles → MOUNTS each batch; gateway 200). **REMAINING (fresh round, tracked task #7 + punch-list):** 13 use-case demo GIFs need ASSET GENERATION (hidden on live by build-docs onerror guard, no broken-icon disaster); deeper schema enumeration (idempotency_key cols / llm_call event row / memory_fts routing section / redundant anchor-map / json-cols); CP-4 fabricated `bene diff` output (needs real 2-checkpoint capture); mem-2/3/4/5; ARCH-6 squirrel_localpath (document-vs-de-register decision); F6 agent_write bytes-vs-chars (server.py code = bene-core lane); cli-reference + skills full pass (bene-core-3 re-reviewing). bene-core export crash CP-3 already fixed by bene-core (`bb6bbc3`).
- 2026-06-16 — Bene 0.30 landing pass SHIPPED (bene-9 editor, bene-core-3 reviewer; per user /goal to vet all customer-facing copy). bene-core-3 shipped 2 kernel fixes to origin (`eaacc0e` query read-only, `caca217` failure-localize-on-real-run) + `bb6bbc3` (bene export) and handed a **27-finding landing punch-list** (8→9 accuracy blockers ACC-01..09) + a separate 43-finding docs punch-list (next round). **bene-9 found + fixed a P0 the punch-list missed:** the EN landing was **BLANK on live** — `@babel/standalone` threw on a bare apostrophe in `'a gate that can't kill its own baseline'` (single-quoted JS string); React never mounted, and every prior "live-verify" missed it because grep matched the `<script type=text/babel>` SOURCE, not rendered DOM (built a real check: extract the babel block + `Babel.transform` it; compiles ⇒ mounts). Fix: `can't`→`cannot`. **All 9 ACC blockers cleared EN+ZH, every snippet ground-truthed against the real CLI:** ACC-01 install (no public path — not on PyPI, repo private → honest `pip install bene` + "coming to PyPI/early access", dropped the 404ing one-click copy, demo kept as "have the repo?" form; publishing is owner's call); ACC-02 SQL (`/tmp/bene-demo-*/story.db` + `title`/`created_at`, drop `summary`/`ts`/`id`); ACC-03 Tab2 (`from bene import Agent`+`.log.propose()` fictional → real `SharedLog(b.conn).intent()/.vote()/.decide()`); ACC-04 (`register_probe()`/`harness.memory.attach_kernel` nonexistent → `Probe(name,[gate],fn).register(store,conn,baseline=…)` / `attach_kernel(harness, memory=…)`); ACC-05 (`mh frontier` + `<search_agent_id>`); ACC-06 (`senses|grep context_budget` never-emitted → `ContextOS().assemble(…).manifest["estimated_tokens"]`); ACC-07 (`bene engram search` no-such-cmd → `bene retrieve`); ACC-08 (BM25/outcome-weighted contradiction reconciled); ACC-09 (`serve --stdio` → `serve --transport stdio`). Plus FIX-2 engrams-default-ON, new `docs/integrating-bene.md` (turnkey vs wire-yourself), copy/preachy nits (9-18) + zh-codeswitch/tigan/headline-gloss/llms-txt-button/繁育→演化 + 3am dedup. **Verified:** babel-compile EN+ZH MOUNT, linter 0 BLOCK/29 WARN, every runnable snippet exit-0 (bene-core-3 independently cross-ran — all PASS). Commits: bene-main `2ebde7d` (origin), bene-site `499cf47`, agentdex-cli `65daa27a` (origin + EdwardTang/agentdex-arena deploy-target). Koyeb POST 18:34 → HEALTHY; **LIVE EN+ZH babel-compile => MOUNTS (blank-page P0 fixed on prod)**, all ACC residue 0 on live, gateway 200. Awaiting bene-core-3's 4 view×lang render-verify 终审 (the one check I can't produce in-sandbox — no CDN egress for headless render). **NEXT: docs punch-list** (`/tmp/bene-9-docs-punchlist.md`, 43 findings) after sign-off. Lesson: a deploy's "live-verify" by grepping served HTML is WORTHLESS for an SPA — the JSX source is in the `<script>` tag whether or not it renders; compile the babel block (or render the #root subtree) — see [[render-verify-bilingual-site]].
- 2026-06-16 — agentdex-roadmap diligence → 2 self-gaps FIXED (bene-core-3, ultracode workflow wf_9cf148bc, 11 agents, real CLI/runner on isolated dbs). User asked whether bene supports agentdex-cli's 5-stage coding-agent roadmap (loop/fault/harden/skills/data). Verdict: every stage has real primitives — **loop = built-in** (engrams + event journal default-on, zero wiring); **fault/harden/skills/data = wire-yourself** (substrate real, but no chained adapter — agentdex must wire 3 recurring glue pieces: structured failure-metadata re-emit, Python-only probe/distill/breed drivers, auto-promote/plasticity scheduling). The diligence ALSO reverse-caught 2 of bene's OWN bugs, both fixed + pushed origin: **(1)** `bene query` read-only was a `startswith()` keyword filter → `WITH … DELETE` / `/* */ DELETE` bypassed it and really deleted rows → engine-level `PRAGMA query_only` (`eaacc0e`, +3 tests). **(2)** row-63 `failure localize` ↔ engrams-by-default composition was an **OVERCLAIM** — the per-turn granule carried only `{granule, iteration}`, tool errors went to the legacy `tool_calls` table, so localize on a REAL run returned `localized:false` (only hand-seeded test traces worked); runner now emits a per-tool trace engram `{tool_name, status, error_message}` at all 3 completion paths → localize works end-to-end (`caca217`, +1 e2e test driving the real runner). Suite **980 passed / 8 skipped / 0 failed**; ruff clean; origin HEAD `caca217`.
- 2026-06-16 — bene-8 → bene-9 baton (Opus 4.8 1M, ultracode; G1 serial-baton 不失忆). Predecessor bene-8 session was already killed by the orchestrator at baton; reconciled against `/tmp/handoff-bene-8-supplement.md` (post-baton delta) — **nothing to redo**. bene-8's post-handoff work was all done: FIX-2 landing re-assert (engrams now default-ON after bene-core shipped #2/6f10abb — Gap 2 card + Contract #2 "by default" + arch mechanism naming `emit_engrams=False`/`kernel.emit_engrams: false` opt-out, EN+ZH), commit `606a043` (linter 0 BLOCK/29 WARN, pushed origin), mirrors synced (bene-site `5299ade`, agentdex-cli `ba02aab8` origin+deploy-target, md5 invariant intact). **The one loose end = live-verify the in-flight deploy** — and on pickup it had FAILED: bene-8's Koyeb POST (15:52Z) of agentdex-arena `ba02aab8` went **UNHEALTHY** ("rollout timed out ~10min waiting for service health"), live site 503/000. **Root-caused it NOT a code/resource regression**: booted HEAD `ba02aab8` locally (dummy `ARENA_ADMIN_TOKEN_HASH` to bypass the fail-closed admin gate) → `GET / → 200` in ~3s, total RSS ~39MB (<<256MB nano) → transient Koyeb cold-build/health-check timeout (the deploy swept in adx-cli's never-before-deployed evolution merges #158/#159/#160, all of which boot clean). Re-POSTed the same HEAD 16:09Z (idempotent; adx-cli-9 was deep in its own Hermes work, not mid-deploy → no race). Result: `koyeb_status=HEALTHY`, site recovered to 200, all routes green (`/bene/`, `/bene/zh/`, `/ladder`, `/enrollment`, `/` arena gateway substance intact); **engrams-default-ON copy LIVE-VERIFIED** on both EN (`By default every run drops a tier-0 trace engram` ×1 + `emit_engrams=False` ×1, stale `attach the kernel, a run drops` ×0) and ZH (`BENE 每次 run 默认就把每轮交互写成` ×1 + `默认每次 run 每轮落一条` ×1, stale `挂上 BENE 的 kernel` ×0); `last-modified: 15:51:36 GMT` proves the FIRST build's image was correct all along — only its rollout health-check timed out (platform wrapper `status` lagged at "deploying" while `koyeb_status` was already HEALTHY; its own message: waits for reverse-proxy refresh). **Daemon transition**: bene-8's daemon (WATCH=`bene bene-core`, pid 2726101) was already DEAD; spawned bene-9 daemon WATCH=`bene` (narrow — resolves the old bene-core double-coverage that bene-core-3 had flagged; bene-core-3 owns `bene-core`) + pidfile `/tmp/sweep-daemon-bene-9.pid` (pid 2632974, log `/tmp/sweep-daemon-bene-9.log`, first sweep clean ctx=14%). Posted A2A bene-9-ALIVE + incident note (harness + bene-core + adx-cli FYI). **Run remains COMPLETE; STANDING BY** per the user's standing "ship 完直接 STAND DOWN, 别开新工作". Deferred eng queue unchanged (agent-loop deterministic Replayer §A.3/§C.2; `bene observe up/down/ensure` langfuse CLI). **Lesson**: a deploy POST returning HTTP 202 is NOT a landed deploy — the Koyeb rollout can still time out UNHEALTHY 10min later; a serial baton MUST re-verify the in-flight deploy reached HEALTHY + live-render the content, never trust the predecessor's "202 accepted" as done (same in-flight-work-died class as the bene-core-3 suite gap).
- 2026-06-16 — bene-core-2 → bene-core-3 baton (Opus 4.8 1M, ultracode; G1 serial-baton 不失忆). Retired predecessor: migrated tmux clients, killed `bene-core-2` session; its sweep daemon was already dead (no pidfile/proc). **Caught a silent gap during retirement**: bene-core-2's final full-suite verification (the in-flight work the handoff handed me) had died mid-run — pid 2452652 was an orphaned bg `&` job (no `disown`/`nohup`), both pytest pids had exited, and `/tmp/fullsuite-final.txt` was frozen at 80% with NO summary line → no green proof actually existed. Re-ran the suite myself, clean: **976 passed / 8 skipped / 0 failed** in 124s (`uv run --extra a2a python -m pytest tests/ -q`, EXIT=0); the flaky `test_concurrent_writes_two_threads` did not trip. Spawned my watchtower daemon (`/tmp/sweep-daemon-bene-core-3.pid` pid 2478117, log `/tmp/sweep-daemon-bene-core-3.log`, WATCH=`bene-core`, broadened unblock/detect/meta lists incl. `bene-core`; first sweep clean at ctx=9%) — added a pidfile the predecessor's lacked, for clean future handoff. **De-dup posted to A2A bus** (`bene-core-3`→harness+bene): `bene-8`'s daemon currently has WATCH=`bene bene-core` (harness-7 stopgap from when bene-core lost its daemon); requested it narrow back to `bene` now that bene-core owns its own daemon, to avoid duplicate handoff-baton firing (the G2 double-orchestration class). **Did NOT re-create bene-core-2's session-only `:07`/`:13` orchestrator crons** — orchestration is the harness lineage's role (harness-7 alive + running its sweep; no durable scheduled_tasks.json → no duplication risk); flagged to harness via bus for coverage confirmation. Confirmed row 63 `bene failure localize` (`f1353b7`) committed + pushed origin, all planned CLAIMS-AUDIT rows worked, 0 open issues — **run remains COMPLETE**. **No open user-gated ask:** the 2026-06-13 "Book shortlist (PAUSE)" below was RESOLVED — user confirmed the 4-book comms shortlist (Mom Test + Pressfield/Zinsser/Dicks), the zh-rewrite-v2 shipped + deployed, and the user said "ship 完直接 STAND DOWN, 别开新工作". The shortlist's (b) product-success bucket (Obviously Awesome / Continuous Discovery Habits / Inspired) was never distilled and is PARKED by that STAND DOWN — resume only if the user reopens it, not a pending decision. **Deferred eng queue unchanged:** agent-loop deterministic Replayer (intel §A.3/§C.2); `bene observe up/down/ensure` self-host langfuse CLI. Per the user's last instruction, bene-core-3 is STANDING BY (no new work).
- 2026-06-16 — Kernel workstream items 8–9 + SKILL.md honesty (bene-core → bene-core-2 baton, Opus 4.8 1M, ultracode). Retired predecessor bene-core (detached, stood down) + transitioned the sweep daemon (`sweep-daemon-bene-core-2`) + re-created the two session-only orchestrator crons (:07 sweep, :13 sync_toc). Picked up the predecessor's in-flight, uncommitted **item 8** from worktree `bene-wt-autonomy-defaults` and finished it: `AutonomyPolicy(default_level=…)` 0..3 config floor (L4 stays human-grant-only) + `autonomy_config_from_config`/`autonomy_policy_from_config[_file]` + `bene autonomy show/grant` CLI; +13 tests; CLAIMS-AUDIT **row 53 → VERIFIED**; commit `9f9362d` (pushed origin). **Item 9** — VEA evidence re-highlighting (`bene/kernel/memory/rehighlight.py` `EvidenceRehighlighter` + `PollutionDetector.rehighlight`/`recover(reask=…)` cheap first rung of the recovery ladder, gated on `evidence_present`; `bene memory rehighlight` CLI); +13 tests; **row 43 → VERIFIED**; commit `5e5037f` (pushed origin). Full suite 947 passed / 0 failed; ruff clean. **SKILL.md honesty sweep** (`3c28762`): the "PLANNED (do NOT claim)" list still flagged 10 capabilities that are all shipped+VERIFIED (plasticity/outcome-ranking 27-29, localizer 30, runner wiring 41/48, scheduled consolidation 26, kernel: 53, VEA 43, signed replay 52, in-episode mutation 35, MemGAS 39) — moved them to Implemented, PLANNED now lists only genuine deferrals; applied to skills/bene/SKILL.md (+ ~/.claude mirror) and site/SKILL.md; deployed live via the 4-mirror chain (bene-site `2bf7f5a`, agentdex-cli `b51f0eb4` origin+deploy-target, Koyeb POST via op-free `~/.ai-builders.json` key → live in ~40s); 4-way md5 invariant `a61a1d21` (3 mirrors + live). **Remaining queue (not done):** agent-loop deterministic Replayer (intel §A.3/§C.2, distinct from shipped consolidation replay); `bene observe up/down/ensure` self-host langfuse CLI (deferred); pre-existing flaky `test_hardening.py::test_concurrent_writes_two_threads` (subtle SQLite write race; conn already WAL+busy_timeout=30s — left untouched, not part of the 0-failed invariant); predecessor's "tasks 4/5/7" numbering was lost with its context.
- 2026-06-11 — Plan created. Interview-first ordering (phases 1–3 critical path before Fri 11:30 PT screen).
- 2026-06-11 — User standing policy applied: autonomous execution in-session (no /goal paste gate). Git re-initialized (corrupt .git archived); baseline commit 6c55fa3.
- 2026-06-11 — Phase 3 kit committed in agentdex-cli (885d4b36) after satisfying its doc-lint hook (frontmatter schema + SLA wording). Phase 4 kernel landed (7ad3747). Phases ran concurrently (no shared files).
- 2026-06-11 — SUPERGOAL_RUN_COMPLETE: 10/10 phases, final audit 85% re-verified, 0 gaps. (This status line was updated late — the audit itself ran at phase-10 close.)
- 2026-06-11 — Post-run work (same session): pre-registered rival benchmark (RIVAL-BENCH-REPORT.md, rounds 1+2; 'supersedes' not claimable per locked rule — A6 relative gate + 4 planned gaps); 3 bench-surfaced defects fixed (B1 evaluator defaults, B3 bug_triage restored+tracked, A6 mirror 4.7x); 62 doc-review findings fixed; predecessor-naming scrub incl. full history rewrite (messages 0 / blobs 0); multi-agent bene skill forged (skills/bene/ + AGENTS.md + ~/.claude/skills/bene, darwin-judged).
- 2026-06-11 — PR babysit: n/a — bene-main has no remote (local-only by design); agentdex-cli work pushed direct to main (fast-forward), no open PRs.
- 2026-06-11 — bene landing PUBLISHED: live at https://agentdex.ai-builders.space/bene/ (mounted additively into agentdex-arena, arena routes untouched; repo EdwardTang/bene-site also deployed over meta-vex slot but that service's rollout-promote is wedged platform-side — build succeeds, traffic never switches; instructor escalation optional). Site test-count stat refreshed 615→699 in all three copies.
- 2026-06-11 — Round-3 (bene-2 session): closed all 4 capability gaps (A1b/A2 weighted ranking, A4 localizer, A5 quality signal) + A6 batched mirrors — every PREREG row now passes under least-favorable readings; locked rule yields "BENE supersedes" claimable for the first time (disclosures: A6 margin ~7%, A1b exact tie). Suite 699 passed. Harnesses tracked under benchmarks/rival/. Demo kit dry-run verified at HEAD (e827c508 in agentdex-cli); bilingual site/ shipped (b14b395); handoff protocol patched with split-brain guard #3.
- 2026-06-12 — Lens-pass-2 rerun (bene-3 session): the predecessor's bene-landing-lens-pass-2 workflow died with its session (kedaibiao + sweep round-2 outputs lost; PG + Nintendo were captured in /tmp/lens2-results.txt and already applied). Re-ran as find→adversarial-verify workflow: 14 confirmed findings, 0 rejected, all applied (ec82a13). Worst: honesty receipts + materials trail (4 spots, EN+zh) attributed Round-1's losing tally to "round-3" and inverted the supersession verdict vs the linked report; docs Quickstart recipe died at `uv sync` (public repo has no package); `uv run bene-mcp` doesn't exist (→ `bene serve --transport stdio`); probes card taught inadmissibility backwards. Deployed (op rate-limited ~90 min; background retry loop landed the key on attempt 40) + live render-verified.
- 2026-06-12 — P0 site fix (bene-3 session): predecessor's docs-view rewrite (55eec7c) updated the zh docs dict + JSX but silently missed the EN docs dict — EN (default lang) Docs tab crashed the React root to a blank page on the LIVE site; found via headless-browser render check during handoff verification. EN dict rebuilt to mirror zh (steps/groups/mcp/research/arch+cta); all 4 view×lang combos render-verified locally; site/check-i18n-parity.mjs added as deterministic guard (en↔zh key-tree diff, exit 1 on drift). Redeployed + live-verified. Lesson: 55eec7c's "EN ↔ zh parity verified" was grep-based, not render-based — greps prove bytes, not rendering.
- 2026-06-12 — bene-3 → bene-4 handoff (this session): retired bene-3 (tmux + sweep daemon), spawned bene-4 sweep daemon (broadened lineage opt-in). Handoff-verification sweep: HEAD 35f5e1a clean, all 4 view×lang combos live-render-verified, i18n parity green (186 nodes). Found+fixed a stale published claim — site/CLAIMS-AUDIT advertised "699 passing tests" but `uv run python -m pytest tests/ -q` now yields 718 passed / 1 skipped / 0 failed (the CLAIMS-AUDIT reproduce-command no longer matched its own number). Root cause: +19 = 22-test Temporal runtime suite, un-gated now that `temporalio` 1.26.0 is installed. Refreshed badge 699→718 (EN+zh) + isTests regex across all 3 site copies; added current dated measurement + env-gating note to CLAIMS-AUDIT; render-verified locally. Also `.gitignore`'d `.serena/` (Serena MCP local cache). Left RIVAL-BENCH-REPORT 699 line (dated Round-3 snapshot, +84 math intact) + SKILL.md (already caveats per-env drift) untouched.
- 2026-06-12 — Landing↔docs inter-navigation fixed (bene-4, commit HEAD): user reported the two surfaces weren't inter-navigable. Audit confirmed: docs→landing worked (generated pages header Landing + Docs index), but landing→docs had exactly one link — buried in the Docs-view sidebar, hidden on mobile, unreachable from the default view. Added 3 symmetric routes (EN/zh, parity 203): top-nav 'Reference'/「文档库」 real link on every view+breakpoint; mobile-visible callout banner atop the Docs view main column; Reference pill in the global footer. Live-verified: 4 docs/index.html links in served HTML, round-trip 200 both directions.
- 2026-06-12 — Per-doc HTML pages shipped (bene-4, commit 8ccef77): user asked for an HTML page per bene-site doc. Survey first: 37 md in bene-main/docs ≡ 37 in bene-site/docs (1 content drift found+fixed: CLAIMS-AUDIT 718-note was bene-main-only while the live badge links the bene-site copy). Built site/build-docs.py — deterministic md→HTML generator (uv run --with markdown --with pygments): light-theme template matching the landing, grouped sticky sidebar, pygments-dark code blocks, mermaid only on t10, 200 internal .md links rewritten to .html, docs/assets copied through, broken-img onerror guard (md files reference never-committed demos/*.gif — pre-existing 404s, same on GitHub), VHS demos injected on README/index (keyless) + cli-reference (litany) + mcp-integration (honesty), per-page 'source: docs/<f>.md — edit the .md, not this file' footer. 37 pages + index at /bene/docs/, SPA Docs sidebar links 'Full reference — all 37 docs' (EN/zh, parity 202). Live-verified (index/cli-reference/CLAIMS-AUDIT/t10/SYNTHESIS all 200 + rendered). Docs-drift management decision: bene-site/docs is a pure mirror of bene-main/docs (never hand-edit); HTML tree is a committed build artifact regenerated via the builder; recommended guards (diff -rq gate + hard-number claims-grep) offered, not yet wired.
- 2026-06-12 — CRITIC LOOP CONVERGED (bene-4, round 3, wf_0aea9192): both lenses returned overall_verdict=satisfied with ZERO remaining majors — each independently re-executed the fixes (grapeot re-ran Recipe 1 verbatim end-to-end + got a real ACCEPT on the probe read-path; paul-graham verified commands against bene/cli/main.py + GitHub API: Discussions 200, receipts 200, two-track CTA live). Landing now only claims what it can prove. Final landing live at https://agentdex.ai-builders.space/bene/ (commit 54bccdd); magicpatterns docs live at https://project-precious-celery-515.magicpatterns.app (editor nr7va6yznlfpu7vc2jb3qk, 7 pages, 3 VHS demos). User work-order complete: WWDC26 research → apple-wwdc2026-keynote skill → 3 VHS demos → benefit-first light-theme landing → magicpatterns docs → critic loop to satisfied.
- 2026-06-13 — Resume: 401 root-caused + adversarial completion-audit + 4 doc-residue fixes (post-COMPLETE polish; this session, Opus 4.7 runtime, ultracode on). Inherited state showed pane 401 "Bad credentials" from a sweep gh call. Diagnosis: shell env held a stale pre-rotation `GH_TOKEN`; `op read 'op://openclaw/gh-pat-europa-admin-no-delete/credential'` returned a value but THAT value is also invalid → the 1Password item itself is rotted (its 2026-06-13 rotation did not propagate to the item; gh's hosts.yml token is the valid one). Patched `~/.bashrc` lines 409-421 to `unset GITHUB_TOKEN GH_TOKEN` FIRST so that when op read returns stale-or-empty, gh falls back to its valid hosts.yml credential instead of 401-ing every call. ESCALATION (user-only): re-rotate the op item to the current valid PAT — Claude must not touch secrets values.
- 2026-06-13 — Adversarial completion-audit (wf_274693ac-15e, 5 dims × adversarial verify): 3 dims came back CLEAN — `claims-vs-code` (every architecture-map subsystem in CLAUDE.md exists+imports; MCP server has 37 tools), `phase-deliverables` (all 10 phase artifacts present in tree), `hygiene-residue` (no NotImplementedError stubs, no leaked secrets, no debug print() in lib code). 4 confirmed gaps — ALL doc/site polish, ZERO in code: (1) docs/README.md:38 published "the full 25 MCP tools" — stale, contradicted its own link target which said 37; (2) docs/README.md:98 + docs/tutorials/t01-bene-meta-harness.md:297 linked to case-studies/cs01-bene-triage-rag-harness.md which was never authored (only cs02 exists); (3) 40 markdown `<img src="demos/bene_*.gif">` refs plus inline pngs (hero-v04, knowledge-compound, compaction-flow/chart) rendered as broken-image icons — STATE.md note 45's "onerror guard" claim was wrong (the guard only fired for the 3 hand-curated DEMO_EMBEDS); (4) ~17 `../examples/*.py` links 404'd on deployed static site because build-docs.py never copied examples/ into site/. The 10/10 COMPLETE claim HOLDS on substance.
- 2026-06-13 — 4 doc-residue fixes shipped (3 tiny commits, local-only per design): 5ef7964 fix MCP count 25→37 in docs/README.md + regen site/docs/README.html; 621f886 drop dead cs01 row from case-studies table + cs01 bullet from t01 Related; 85b3006 site(build-docs.py) — extend rewrite_links() to inject `onerror="this.style.display='none'"` on any `<img>` without one (covers all 40 demos/*.gif + the 4 inline pngs uniformly; hand-curated DEMO_EMBEDS unaffected because they're injected after rewrite_links runs) + add examples/ copytree alongside docs/assets so `../examples/*.py` links resolve at site/examples/ (17 example scripts now ship as committed build artifact alongside site/docs/). Regenerated all 37 doc pages — 21 changed (the ones with `<img>` tags). GATE re-green: pytest 741 passed / 1 skipped / 0 failed (92.8s; invariant unchanged), ruff clean, i18n parity 203 nodes identical. STATE.md test count refreshed 718→741 (the +23 came from the Postgres-engram adaptor merge that landed after the 2026-06-12 718 measurement).
- 2026-06-13 — Wider claim-audit milestone (wf_8d707f30-615, 7 dims × adversarial verify): 3 dims CLEAN — `rival-bench-supersession` (the locked-rule supersession verdict + A6 ~7%/A1b exact-tie disclosures + dated 699+3 snapshot all hold), `architecture-invariants` (engram tiers 0-4, autonomy ladder L0-L4, 4 trust signals + composite, eval-probe ACCEPT/REJECT/VOID with hash-locked kill gates, and MCP 37 tools / 9 categories all match code), `hard-number-drift-sweep` (no other undated/contradicting numbers in docs/ + site/). 4 confirmed gaps: (i) `docs/design/CLAIMS-AUDIT.md:81` called `bene/benchmarks/` docstring-only — stale because the `bug_triage` sub-package (12 modules, ~3041 LOC) was restored in 4c85fef. (ii) `https://agentdex.ai-builders.space/bene/` returns **HTTP 404** — every BENE candidate path 404s; host root /  is 200 but only serves the agentdex-arena JSON API ({"ok":true,"service":"agentdex-arena","lanes":["sandbox","rated"]}). STATE.md notes 39/46/51 advertised the mount as live; reality is the additive `/bene/` mount is gone (rolled back, evicted, or the wedged meta-vex slot promote per note 39 never resolved). (iii) VHS demo `.tape` sources for demo-keyless/litany/honesty were never committed — only the GIF outputs are in `site/assets/demos/`; "no recipe to reproduce" on the next demo refresh. (iv) `~/.claude/skills/bene/SKILL.md` mirror was the older Jun-11 absolute-path version while the in-tree Jun-13 version uses relative paths.
- 2026-06-13 — Milestone-audit fixes: (i) 2f0151f docs(claims-audit): close BENE-13 namespace note — `bene/integrations/` still docstring-only (correct), `bene/benchmarks/` prediction fulfilled by `bug_triage`. (iv) Synced `~/.claude/skills/bene/SKILL.md` from the in-tree Jun-13 version (user-local untracked, no commit; byte-identical post-cp). NOT applied autonomously: (ii) the live-404 fix lives in `/home/admin/gh/agentdex-cli` (cross-repo + outward-facing deploy + the op-stored PAT this session diagnosed as rotted + the wedged platform-side rollout-promote noted 2026-06-11) — USER-ACTION REQUIRED: redeploy from agentdex-cli OR mark the URL no-longer-published in the project narrative. Magicpatterns docs at https://project-precious-celery-515.magicpatterns.app **IS** still live (HTTP 200 GET, served by Magic Patterns CDN). (iii) `.tape` recipes — not reconstructed autonomously (would re-create a false "recorded against HEAD with no edits" claim); tracked as a fix-action for the next demo refresh.
- 2026-06-13 — `/bene/` 404 CLOSED — landing back live (cross-repo fix, user-authorized). Diagnosis: the agentdex-arena Koyeb service had been force-pushed from the phase-2 spike (`/tmp/agentdex-arena/app.py` + `StaticFiles('/bene')` + `site/`) to the full agentdex-cli gateway (`packages/agentdex_arena/src/agentdex_arena/gateway.py` — `{"lanes":["sandbox","rated"]}` shape, `/ladder` /`enrollment` /`mcp` routes). The `/bene` mount was a casualty of that switch; the meta-vex platform wedge was a red herring (the wedge was on a DIFFERENT slot, `EdwardTang/bene-site`, not the arena slot). Fix (agentdex-cli commit `6f913baf`, tiny 3-file delta + the 67-file site/ tree): (a) Dockerfile adds `COPY site/ ./site/`; (b) gateway.py imports `StaticFiles` and `app.mount("/bene", StaticFiles(directory="site", html=True))` after the `/mcp` mount, guarded by `Path("site").is_dir()` so local-dev runs without the bundle don't crash; (c) site/ is the 4th copy in the bene-main → bene-site → agentdex-arena → agentdex-cli sync chain. Verified before push: `from agentdex_arena.gateway import create_app` OK; agentdex_arena tests 44/44 pass; TestClient on the mount alone → /bene/, /bene/index.html, /bene/docs/, /bene/docs/README.html all 200; ruff clean. Pushed to `good-night-oppie/agentdex-cli` main + fast-forward pushed to `EdwardTang/agentdex-arena` main (the Koyeb deploy target, 3 commits propagated incl. 2 gym-bot commits #83/#86). Triggered Koyeb deploy via POST https://space.ai-builders.com/backend/v1/deployments with 1P-stored `spaces.ai-builders.com-api-key` bearer (op read worked this round — distinct PAT from the rotted gh PAT diagnosed earlier this session). Build finished at T+150s; live verification: /, /bene/, /bene/index.html, /bene/docs/, /bene/docs/index.html, /bene/docs/README.html, /bene/docs/design/CLAIMS-AUDIT.html, /ladder, /enrollment ALL HTTP 200. /bene/docs/README.html shows "the full 37 MCP tools" (the 25→37 fix from earlier this session propagated). /bene/examples/memory_search.py 200 (the build-docs.py examples/ copytree fix also propagated). Gateway substance (gym bots, audit, enrollment) intact — no rollback. STATE.md milestone-audit gap (ii) is now CLOSED.
- 2026-06-13 — Round-3 claim audit (wf_3bb70e27-c05, 6 dims × adversarial verify): 3 dims CLEAN — `site-sync-invariant` (bene-main/site/ ↔ agentdex-cli/site/ byte-identical, 67/67 files), `live-render-spotcheck` (live deploy fresh — 37 MCP tools + onerror guards both present in served HTML), `route-and-asset-resolution` (internal doc-to-doc and asset links resolve). 9 confirmed gaps across 3 buckets — ALL closed: (A) public-exposure-scan: 3 dev-only files (test-harness.html medium · build-docs.py low · check-i18n-parity.mjs low) were riding the site/ sync into the /bene/ deploy. Fix: agentdex-cli commit `185e9e5a` Dockerfile `RUN rm -f` after the site/ COPY strips them from the deploy image; source-of-truth in bene-main/site/ unchanged; sync invariant unchanged. (B) cross-surface-consistency: `EdwardTang/bene-site/docs/architecture.md` said "17 tools across 6 categories" + `docs/mcp-integration.md` said "18 tools" — stale (canonical: 37/9). Discovery: `/tmp/bene-site` already had commit `d1781c8 docs: de-kaos rewrite` locally with the correct 37/9, just never pushed to origin. Plain `git push origin main` closed both gaps; CDN refreshed in 15s; verified `raw.githubusercontent.com` returns 37/9 across both docs. (Same anti-pattern as the earlier `/tmp/agentdex-arena` discovery — local commits sitting unpushed.) (C) memory-and-deploy-pipeline-drift: 4 stale memory entries about the deploy chain. Refreshed: `bene-site-deploy-pipeline.md` (3-copy → 4-copy + the canonical-vehicle-shift to the agentdex-cli mirror flow + the dev-file strip + `test-harness.html` no longer expected on live), `MEMORY.md` one-liner (matching update), and the agentdex-cli memory file `project_spaces_platform_contract.md` (added the mirror-flow note so a future agent doesn't push directly to `EdwardTang/agentdex-arena` and get silently force-overwritten). User-local memory files — no commit. Deploy verification: POST → Koyeb rebuild T+30s end-to-end (tiniest diff yet); live verified — /bene/ 200, /bene/test-harness.html 404, /bene/build-docs.py 404, /bene/check-i18n-parity.mjs 404, gateway substance (lanes shape, /ladder, /enrollment) intact.
- 2026-06-13 — User rejected the zh-rewrite from earlier today (`35c6f44` / `258b43e` / `02df746` already merged to bene-main main): "harness is one SQLite 这种煞笔话真的不能忍". Diagnosis is correct — I replaced "整套 harness 就是 SQLite" with "一个 agent = 一份 SQLite", same reductive X=Y句式, just narrower scope. The category error is "AI-slop = feature list with no model of reader's brain". User-prescribed remedy: read Lenny Rachitsky's *Essential books for product builders—part 1*, distill the communication + product-success-rate buckets, then rewrite with real evidence base. Workflow: STEP 1→5, no touching site/index.html until STEP 4, PAUSE after STEP 2 book shortlist for user confirmation. STEP 1 done: fetched the article via `mcp__mysearch__extract_url` (Firecrawl out of credits, fell back to Tavily), 9 buckets / 27 books visible (last 2 buckets paywalled — article subtitle says "36 timeless books" total). Raw extracted text saved at `.supergoal/research-lenny/source.md` for STEP 3 reference. STEP 2 PAUSED awaiting confirmation — see "Book shortlist (PAUSE for user)" below.
- 2026-06-15 — Recipes 重写 + 流程图 + sibling adx-cli-7 来信回复 (bene-7 session)。user 两条指令叠 stack：(a) "八个 recipe，方法论先落成动词：方法论都有对应的 bene 子模块" + (b) "为这些 recipe 配 factory ai style 流程图"。8 个 recipe 全量重写：title 改成 "verb : bene.module" 形式（R1 "Recover from a bad turn : bene.checkpoints"、R2 "Coordinate two agents : bene.shared_log"、R3 "Gate the next promote : bene.kernel.eval"、R4 "Compound memory across runs : bene.kernel.memory"、R5 "Keep context inside budget : bene.kernel.memory.ContextOS"、R6 "Escalate autonomy by probe : bene.kernel.trust + .harness"、R7 "Breed a better strategy : bene.kernel.evolve"、R8 "Plug BENE into your agent : bene.mcp"；zh 同步），每个 recipe 加 3-stage flow chips (factory.ai 调子：Geist Mono、burnt-orange 箭头、中间格 highlight)。`m` (module) 字段单独一行 burnt-orange，flow chips 在它下面横排。bene-main `168e024`、bene-site `e56931c`、agentdex-cli + arena `a007e4ff`。也包含同 session 下的 demo-label dedup (`8ec3d35` mirror `221bf7f` / `046972e7`)：4 个 GapDemo 顶的 "demo — 对 bene-main HEAD 实跑" / "demo — recorded against bene-main HEAD" 复读太多 → per-card label 删掉，section-level claim 在 The Gap h2 下面写一次。**deploy 卡住 → 67min 后用 env-var 绕过**：op service account 自 03:21 起 rate-limited，retry loop 跑了 25+ 次（60s → 180s 间隔），到 04:25 还卡着，并行 adx-cli 也在同时打 op 拉 GitHub PAT，两边一起抢额度（memory 原本写「10+ min」，这次远超，刷新为「60+ min 也有可能」）。用户直接给了 `~/.bashrc` 里新加的 `SPAC_AI_BUILDERS_API_KEY` env var——`source ~/.bashrc && curl -H "Authorization: Bearer $SPAC_AI_BUILDERS_API_KEY"` 完全绕过 op。04:28:07 POST 202，T+30s 落地，`last-modified: 04:28:38 GMT`。secrets discipline：curl 直接 inline 用 env var，从来没 echo 到 pane / log，输出过滤所有 `[A-Za-z0-9]{30,}` 长串作为 masked。memory `bene-site-deploy-pipeline` 已更新记下这条绕过路径。**sibling 来信处理 + close**：adx-cli-7 03:30 左右丢过来一封 P1-P3 跨 lineage planning 请求 —— 11d 签名 replay (要 deterministic replay CLI on bene-main) / 11f 回归门 (要 bene.yaml `kernel.consolidation_schedule` wiring + 外部 scheduler) / M7 learned seeds (要 auto-promotion). 04:32 把回复 paste 进 adx-cli-7 pane (tmux load-buffer + paste-buffer + Enter)。回复立场：bene-main 这 phase 在 polish 不开新 CLI feature，substrate 都 VERIFIED 在位 (CLAIMS-AUDIT line 52 deterministic replay manifest 已 emit、line 26+53 ScheduledConsolidator planner 已进)，adx-cli 自己接消费层；反推 thin-CLI PR 进 bene-main 我接 (≤200 行紧贴 substrate)；M7 L0-L3 auto-promotion clear go，L4 必须 human 是硬契约 (CLAIMS-AUDIT line 42 + landing R6) 不破。04:41 adx-cli-7 回 close：自己写消费层 / BadgeAuthority key 复用进 11d adversarial / 11d 4 个 tiny PR cadence 2-3 天 / 反推 thin-CLI PR 接在 11d.4 后 / M7 L0-L3 不动 L4。04:43 我送 close-ack standing by。Cross-lineage 这件事就此 closed，等他们的 PR。
- 2026-06-15 — zh 内容三处小修 (bene-7 session, /stop-slop-zh 触发的扫雷收尾)。原本两处，用户接着又指出第三处误译（接管子 → 接入数据），一并修了。用户 `/stop-slop-zh` + 中文人话 directive 之后，对 zh landing 跑了 slop 词扫描——大体干净（赋能/对齐/维度/无缝/全方位 全 0；codex revision 留下的大厂工程师口吻还在），但顺手捞出两条真问题。**(A) zh-banner 老橙色**：`build-docs.py:235` 的 zh docs banner 还在用 `rgba(255,92,36,...)`（老 traffic-cone red #FF5C24），跟 2026-06-15 redesign-v2 切到的 `#EE6018` 不一致。42 个 zh 文档页跟着错了一周。一行 RGB 改成 `238,96,24`，regen 42 页，3-mirror sync + Koyeb POST。bene-main `1be9078`、bene-site `9cb2b89`、agentdex-cli + arena `1ddca342`。**(B) 「看板」误译**：用户当场指出 dashboard 不能译成「看板」（Kanban 是带列、有泳道的项目管理板），应该是「仪表盘」。zh landing 两处都改了（hero 段的「不用盯着还在转圈的看板」+ Gap 4 的「不是一个还在转圈的看板」）。zh-translation-style.md memory 也修了，未来不会再错。bene-main `48f6f13`、bene-site `e0e53a0`、agentdex-cli 推到 main 时撞上并发分支（gym-bot 新开的 `11c1/badge-authority` 把 HEAD 切走了），先 reset 那个分支的误塞 commit，切回 main cherry-pick，origin + deploy-target push 都到 `ae2f960a`。Koyeb POST 03:05:48 → live verify。**取舍说明**：`闭环` 在 zh landing 出现 1 次（「verify 没跑，闭环就没关上」）——stop-slop-zh 默认禁这个词，但这里是具体上下文（loop 没关上），不是抽象 SaaS slop，保留；`三个月后` 4 次 + `三个面` / `三种 verdict` 各 1 次，全是具体时间 / 真实枚举（Python/CLI/MCP 三个 substrate；ACCEPT/REJECT/VOID 三种 verdict），不是机械 tripling，也保留；`智能` 5 次全是「多智能体」「智能体」（agent 的中文译法），不是空心 SaaS 形容词。**(C) 接管子 → 接入数据**：Contract section 收尾原 EN 是 "The rest is plumbing." 直译成「剩下那些活儿，基本都在接管子」，中文工程师不会这么讲。用户原话纠正成「接下来就只剩下接入数据了」——更直、更具体。bene-main `b92f58a`、bene-site `f0c9746`、agentdex-cli + arena `eddef755`。zh-translation-style memory 加了 plumbing 这一行。**最终 live verify**：仪表盘 = 2 ✓ / 看板 = 0 ✓ / 接入数据 ≥ 1 ✓ / 接管子 = 0 ✓ / zh-banner 新橙 ✓ / 老橙 = 0 ✓ / 4 view×lang 全 200 / sync invariant md5 `8af8a9c7` 一致。**agentdex-cli 分支风险**：第二次 push 时撞上并发 sibling 创建的 `11c1/badge-authority` 分支（gym-bot 在跑 BadgeAuthority feature），HEAD 被切走，我误塞了一笔 commit 到那个分支。reset 那个分支回 sibling 原状（`98377a25`），切回 main cherry-pick 上去。规避方法：每次推 agentdex-cli 前先 `git checkout main`。
- 2026-06-15 — Factory.ai-grammar refactor TAIL CLOSED (bene-7 session, post-deploy followups under standing policy `feedback-fix-all-before-moving-forward`). Live-verify of the redesign-v2 deploy turned up two real residues; user cited the standing policy ("do the whole queue, don't ask"), so bene-7 worked both top-to-bottom as tiny PRs. **Task 7 — font preload residue**: every one of the 84 generated doc pages was still requesting `Inter` + `JetBrains+Mono` + `Playfair+Display` from fonts.googleapis.com via `build-docs.py:283`'s `<link>` preload, even though every CSS rule already used Geist + Geist Mono and the commit message of `e523aa7` explicitly claimed "Playfair Display serif removed". Wasted bandwidth and a documented-claim contradiction. One-line URL swap + preconnect hints to match `site/index.html` + regenerated 84 pages. bene-main `d1eb76f`, bene-site `11acd03`, agentdex-cli + arena `89ad4df5`. First Koyeb POST went HTTP 202 but never landed (still showed `last-modified: 01:51:24 GMT` after 14 min); re-POST at 02:20:10 succeeded in ~4 min — live now has `last-modified: 02:20:12 GMT` + content-length 56234→56334 (+100B for the URL swap + 2 preconnects). Verified across 6 doc pages (4 EN + 1 zh + index): `geist-preload=2 / old-trio=0` on all. 4 view×lang combos still 200. **Task 8 — obsolete i18n parity script**: `site/check-i18n-parity.mjs` was added 2026-06-12 (commit `ec82a13`) to guard the old `const dict = { en, zh }` architecture by key-tree-diffing the two language subtrees. That architecture was retired 2026-06-14 (commit `5db3e2d` + the build-docs.py zh-mirror change in `3a32d31`): EN and zh are now 2 independent self-contained HTML files. Running the script today throws `Error: dict object not found` at line 19. `git rm` from bene-main `f58f9ae`, mirror cleanup bene-site `2baaa8f`, agentdex-cli `7219d336` drops the now-orphan `check-i18n-parity.mjs` entry from the Dockerfile rm-list AND from `.gitignore`'s "BENE landing dev-tooling" block (left `build-docs.py` + `test-harness.html` strips intact — those bene-main artifacts still ship and still need stripping). Replacement safeguard: `render-verify-bilingual-site` memory now points at headless-chromium render across all 4 view×lang combos — a runtime check that survives the new architecture. **Both Koyeb POSTs worked** (2nd one was the retry after the silently-skipped first; 3rd was a no-op for task 8 since Dockerfile-only changes don't alter the deploy artifact). **Standing policy saved**: bene memory now has `feedback-fix-all-before-moving-forward.md` so future bene-N sessions don't repeat the "fix now or defer?" stall. Sync invariant intact (md5 `98da9790` on site/index.html across all 3 mirrors).
- 2026-06-15 — Factory.ai-grammar redesign-v2 SHIPPED LIVE (bene-7 session, picks up the in-flight work bene-6 left at handoff). Predecessor bene-6 landed the actual rewrite commit `e523aa7` (Geist + Geist Mono replace Inter/JetBrains Mono/Space Grotesk; light cream #F0EEE8 becomes the default theme; burnt orange #EE6018 replaces traffic-cone red; Contract/Recipes/Limits flip to warm ink inverted for tempo; serif Playfair Display removed; corner-radius scale tightened to 3/4/6px; flat — no drop shadows) but stood down before the 3-mirror sync + Koyeb POST. Bene-7 picks up the deploy chain. **Defensive rescue first**: inherited tree had untracked `site/assets/Documents/` (Eddie's Qumulo separation agreement + 2026 benefit guide + RGA PPO SBC + DocuSign-signed agreement) sitting one `rsync -a site/ → mirrors/site/` away from being silently bundled into agentdex-cli/site/ → public GitHub → public Koyeb URL. Same risk on `site/index.html.pre-redesign.bak`. Moved both to `/home/admin/_attic/bene-site-stray-2026-06-15/` (preserve, don't delete); added defensive `.gitignore` entries `site/assets/Documents/` + `site/**/*.bak` (bene-main commit `f222552`). Confirmed neither mirror was previously leaked. **Sync chain**: (1) `/tmp/bene-site` rsync + commit `d5e098f` + push to `EdwardTang/bene-site` ✓; (2) `agentdex-cli` rsync — first commit attempt blocked by doc-lint `DOC-LINT-001` (parent commit `912abd3d` had "gap" in subject → triggered agent-fix detector; staged set had `site/examples/*.py` matching CODE_EXTS + no `docs/**` → BLOCK). Root cause: the 2026-06-14 `61e525e3` carve-out only covered per-file md lint at `_is_deploy_artifact()`, not the `check_commit_shape()` rule. Extended the carve-out: site/ paths now excluded from `src_files` in commit-shape, AND switched `startswith` checks to use a `_rel()` helper because `get_staged_files()` returns absolute paths (so the original `str(p).startswith("site/")` would have matched nothing — pre-existing latent bug now fixed for both `src_files` and `doc_files`). Patch + DEV_SETUP.md note + the full site/ sync bundled into one commit `82dd598e` to satisfy the lint's own rule. Pushed to `good-night-oppie/agentdex-cli` origin + fast-forward push to `EdwardTang/agentdex-arena` deploy-target. **Koyeb POST**: 1P key `spaces.ai-builders.com-api-key` op-read first-try, HTTP 202 (~61s streaming hold), build T+~10 min (cold build — site/ tree got a lot of new files including the design-explorations/v2-factory-grammar.html showcase from `3967bd3`). **Live verify**: all 15 sampled routes 200 (/, /bene/, /bene/zh/, /bene/docs/, /bene/docs/README.html, /bene/docs/architecture.html, /bene/zh/docs/, /bene/zh/docs/README.html, /bene/SKILL.md, /bene/llms.txt, /ladder, /enrollment, 2x gap demo gifs, /bene/design-explorations/v2-factory-grammar.html); EN landing has the new RGB tokens `238 96 24` (#EE6018) + `240 238 232` (#F0EEE8), Geist x6, Geist Mono x2, zero JetBrains Mono / FF5C24 / 0A0A0A leftovers; zh landing same tokens + Noto Sans SC CJK fallback x3; sample doc page architecture.html inherits cream + Geist + Geist Mono x8, zero Playfair Display; 4 VHS demos (gap-1-killgate / -2-engram / -3-trust / -4-sqlite) present in both languages; 2026-06-14 大厂中文黑话 codex-revisions still live in zh (卡点 x8, 看板 x2, 线上 x4, 信任 x2, 团队 x1); Copy-llms.txt button x2 + SKILL.md + llms.txt refs intact; theme toggle (3 hits) + lang toggle (EN→`href="zh/"` relative, ZH→`href="../"` relative — both work because of relative-path symmetry); gateway substance untouched. **Sensitive-file leak audit**: confirmed `/bene/assets/Documents/` (404), the actual Qumulo PDF URL (404), `.bak` (404), `build-docs.py` (404), `test-harness.html` (404) — Dockerfile rm-list still stripping the 3 dev files; my defensive moves prevent the Documents/ + .bak leak. **Known follow-up**: `site/check-i18n-parity.mjs` is obsolete after the 2026-06-14 URL-based i18n migration (it greps for `const dict = {` which no longer exists in the redesign — EN and zh are now 2 independent self-contained HTML files). Vestigial; stripped from deploy by Dockerfile rm-list, so user-invisible; remove on next sync cycle. Mirror commits: bene-site `d5e098f`, agentdex-cli `82dd598e`. **Sync invariant**: bene-main HEAD `site/index.html` byte-identical (md5 `98da9790`) across all 3 mirrors.
- 2026-06-15 — 重录 4 个 gap demo + 蒸馏 harness-engineering 录制原则 (bene-6 session, follow-up to 2026-06-14 first cut). User reported "字母间距好大"; 根因诊断 — JetBrains Mono 没装在 dev box，VHS fallback 到 default sans-serif 拉宽了 tracking。装 ttf 到 ~/.local/share/fonts/jbm/ + fc-cache + verified `fc-match "JetBrains Mono"` 返回正版。同时 user 要求"用 harness-engineering repo 视频的经验教训"重做：扫了 `/home/admin/gh/harness-engineering/data/silver/lists/vibe-coding/06_OpenAI提出新概念 Harness Engineering【详细解读】` 等 transcripts，蒸馏出 5 条 demo 工艺规则—— (1) "上下文是稀缺资源"→VHS `Hide` 把 setup 命令藏掉，viewer 只看到 question + punchline; (2) "渐近式披露"→ 一个 demo 一个 punchline; (3) "找到那个 moment"→3-4s Sleep 让眼睛落在结果上+1 行尾注释; (4) "减 token、改得准" (视频 #14 CodeGraph 原则)→输出 pipe 过 head/grep/helper 预压到 5 行可见; (5) "实跑、零输出修改"→所有 prompt 下面的字符都是 binary 真的吐出来的。落地：4 个 tape 全部从 v1 重写——`Hide` 块吃掉 cd/init/demo 的 4-5s 噪音；punchline 各自一条（Gap1: `story-probe -> ACCEPT` 一行 via `grep -B1 -A1 probe`；Gap2: tier 分布 5 行；Gap3: 5 行对齐的 4 signal+composite via `_show_trust.sh`，v1 是 28 行 raw JSON；Gap4: `ls -lh` + 3 行 stdlib sqlite3 读出）。helper `_show_trust.sh` 改用 argv 传 JSON 避开 python 3.11 f-string-backslash SyntaxError。新增 `site/demos/tapes/README.md` 记录 font 装法 + 5 条规则 + 引用 transcripts + bene CLI 演进后 re-record 协议。GIF 大小变化：gap-1 145KB→100KB (-31%); gap-2 119KB→174KB (+46%，tier 分布表更密); gap-3 120KB→118KB; gap-4 115KB→139KB (+21%)。Commits: e962f6d (4 重录 + tapes + README)。Mirror chain: bene-site 6738783, agentdex-cli 912abd3d。Koyeb 8 min 冷构建后 live verify：4 个 gif 全部 byte-for-byte 匹配 local；整站 200。
- 2026-06-14 — Per-gap VHS demos (bene-6 session). User directive: "每个卡点都用一个 Terminal VHS recording to show how bene address the problem." Recorded 4 short demos against bene-main HEAD with no output edits — every command shown in the GIF actually runs and produces the displayed result. Each tape ~12-18s. Source .tape files + helper bash scripts committed alongside the .gif outputs (closing the round-3 audit's "VHS .tape sources never committed" gap from 2026-06-13). Tapes (site/demos/tapes/): (1) gap-1-killgate.tape → `bene init && bene demo --no-ui` seeds the kernel story including a probe; `bene experiments ls` shows the verdict "story-probe -> ACCEPT" as an engram row. (2) gap-2-engram.tape → `bene query` with GROUP BY tier + sample row per tier reveals engrams across tier 0 (trace / eval / trust), tier 1 (episodic), tier 4 (strategic / genome). (3) gap-3-trust.tape → `bene trust <agent> | python -m json.tool` reveals the 4-signal composite (verification_coverage 0.3 / audit 0.25 / checkpoint 0.2 / outcome 0.25) — each derived from the audit trail, not a config flag. (4) gap-4-sqlite.tape → `ls -la $DEMO_DB` shows a real 332KB SQLite file; a 4-line python script reads engrams via the stdlib sqlite3 module — no client library required. Helper scripts (`_extract_demo_db.sh`, `_show_engrams.sh`, `_show_trust.sh`, `_show_sqlite.sh`) keep VHS Type strings free of nested-quote escapes (first-batch tapes 2/3/4 failed on `\"SELECT...\"` parse; helpers solve cleanly). Landing wiring: new `<GapDemo gif=… caption=…>` React component (EN + zh independent definitions); embedded under each Gap card's paragraph; terminal-chrome styled with 3 traffic-light dots + "demo — recorded against bene-main HEAD" / "demo — 对 bene-main HEAD 实跑" label; `<img loading="lazy" + onError remove>` so GIFs lazy-load below the fold and any 404 silently removes itself. GIF total ~500KB extra weight on Gap section, all lazy-loaded. .gitignore *.db under site/demos/tapes/ to keep future re-records clean (cwd-during-record creates a bene.db side effect). Commits: 838368d (demos + tapes + landing wiring), 1a4ed5a (.gitignore defensive). Mirror chain: bene-site 04593f2, agentdex-cli 07fcf598 (origin + deploy-target). Koyeb POST → 8-min cold build → live verify all green: /bene/ + /bene/zh/ 200; all 4 /bene/assets/demos/gap-*.gif 200; landing has 4 GapDemo embeds; zh landing has 4 GapDemo embeds; SKILL.md / llms.txt / ladder / enrollment / agents-as-users endpoints all 200 unchanged.
- 2026-06-14 — codex-校正 zh landing (bene-6 session, continuation of the 6-lens audit + zh i18n batch). User directive: "每一段翻译都让 codex 从'地道而有趣的中文文字'改进，然后参考 codex 的纠正" + "对于非技术类词汇，直译成大厂中文黑话，例如 gap->卡点，技术类就分成半翻译或不翻译加注释：例如 Agent Stack -> Agent 技术栈, 不翻译但注释：engram->记忆轨迹". Workflow: 把 zh landing 全部 26 段（不算 React 组件代码）分 3 批喂给 `codex exec --skip-git-repo-check --color never`，每批让它从大厂工程师写技术博客的口吻给出修订版；编辑层取 codex 的语调和动词选择，剔除 codex 倾向于对每段都重新注释的过度行为——技术词只在首次出现处加一次「」中文短注，重复出现不再注释。Style 规则写入 user-memory `zh-translation-style.md`（首条同款 user-set memory）。Commits bene-main HEAD（自这一节）：cb005a4 zh landing codex-revised；mirror chain：bene-site 6ea6775、agentdex-cli ef95827e（origin + deploy-target）。Koyeb 用了 ~8 min 才把新版本推上去（之前几次秒级 cache 让我以为 30s polling 够，这次是真的冷构建）；最终 live verify 全绿——每一处 codex 修订（"把线上带沟里"、"只说明它在题库里表现不错"、"打开很快，忘掉更快"、"共吃一份 bene.db"、"别想混成 ACCEPT"、"只要能起名字"、"闭环就没关上"、"五件家伙全摸一遍"、"含金量"、"别硬上"、"这个顺序才稳" 等 20+ 短语）均 grep 命中 1 次。EN audit fixes（meta-desc / Keep your crew / copy install）保持原样；agents-as-users endpoints (/bene/SKILL.md, /bene/llms.txt) + zh docs tree (/bene/zh/docs/*) + gateway substance (/ladder, /enrollment) 全 200。Style 规则要点：非技术词→大厂黑话 (gap→卡点 / ship→上线 / bad ship→上线翻车 / dashboard→看板 / production→线上 / crew→团队)；技术词→半翻译 or 保留 + 首次注释 (engram→engram「记忆轨迹」、probe→probe「探针」、harness→harness「调度框架」、VFS→VFS「虚拟文件系统」、SharedLog→SharedLog「共享日志」、provenance→provenance「溯源」、composite→composite「合成分」)；agent/prompt/API/config/Framework/provider/YAML/diff/restore/run/promote 这些常识词不再注释；禁用翻译腔、AI 腔、X=Y、叠词；句感"工程师喝咖啡讲故事"。
- 2026-06-14 — 6-lens audit fixes + zh i18n batch (bene-6 session, same session as the community-peer + agents-as-users work). An inter-session 6-lens review (Pressfield / Zinsser / Mom-Test / Storyworthy / Karpathy-X=Y / UX-IA + adversarial 1-of-2 skeptic verify) returned 6 MUST_FIX against the EN landing (1be6d04 baseline): (1) meta-description X=Y stack; (2) hero body X=Y product def after H1; (3) Contract H3s parallel X=Y / noun-chain (incl. the user-named canonical "Trust is computed, not declared"累犯); (4) Gap section's pre-emptive apology between H2 and first card; (5) Comparison lead self-praise, ignoring reader's toolbox; (6) Hero CTA missing copy-install for first-touch. Shipped as 6 tiny commits b887992→9768839 (meta-desc → hero body → contract H3s → gap apology delete → comparison lead → hero copy-install). Each direction choice documented in commit body. Same direction choices mirrored into the in-progress zh landing so it doesn't carry forward rejected English. Parallel: shipped zh i18n batch — site/zh/index.html (4-book methodology applied: Mom Test scenario-first, Pressfield Hook→Build→Payoff, Zinsser active-verbs, Dicks moment-of-change), build-docs.py extended with `lang` kwarg so it emits a /zh/docs/ mirror tree on every build (42 zh doc pages); each zh doc shows a translation-in-progress banner explaining the 4-book bar + EN body + EN toggle back to /bene/docs/<same>.html; each EN doc gains a 文 pill in nav routing to /bene/zh/docs/<same>.html. URL-based i18n (no dict/localStorage), SEO-friendly, deep-linkable in either language. Commits: 5db3e2d zh landing, 71a9e8c zh residual scrub, 3a32d31-family build-docs zh tree. Sync chain (3 git mirrors): bene-site ee3bc88, agentdex-cli 30d7ef5c (origin + deploy-target). Koyeb POST → build T+30s → live verify ALL GREEN: EN meta-desc scene-first + 03:00 hero scene + L4-by-name contract + comparison-opens-with-crew + copy-install button + 文 toggle ALL PRESENT; ZH meta-desc + 03:00 scene + L4 by-name + crew-留着 + 复制命令 + EN toggle ALL PRESENT; /bene/zh/docs/ + /bene/zh/docs/architecture.html + /bene/zh/docs/benchmarks/COMMUNITY-BENCH-REPORT.html all 200; banner + cross-toggles correct depth-aware path; gateway /ladder /enrollment /bene/llms.txt /bene/SKILL.md all 200 unchanged. Old "None of these is a knock" apology = 0 hits live. Queued (not in this session): per-page zh.md translations of the top docs (README, architecture, cli-reference, mcp-integration, COMMUNITY-BENCH-REPORT) under the same 4-book bar; banner stub disappears for any page where a zh.md sibling lands.
- 2026-06-14 — Community-peer reframe + agents-as-users landing batch (bene-6 session, this session). User-driven, sequenced over the session: (a) "no rival in the long run and co-opetition: instead of saying RIVAL, let's use community" → "we are one OSS community" — drop adversarial framing throughout repo + docs + landing; (b) mobile nav fix + prominent Docs button — top-nav Docs/GitHub were `hidden md:flex` so mobile users saw only the BENE brand; (c) ship `/bene/SKILL.md` so user agents can fetch the full skill and "run BENE itself instead of you babysitting it"; (d) ship `/bene/llms.txt` (llms.txt-format index) + "Copy llms.txt" button on landing + every doc page so the agent gets the canonical path map in one click; (e) SKILL.md heartbeat self-update protocol — re-fetch every 24h, replace on diff, fall back to cached on network failure. Commits (bene-main): df43aff bench rename, 2298f56 report rename+reframe, 760ae07 GAP-AUDIT, 33339a6 BENE2-DESIGN+DESIGN-RATIONALE+AGENTS+SKILL sweep, c204319 site/index + docs HTML regen, a35d930 mobile-nav, c10ea5c Docs ↗ pill + Hero CTA + SKILL.md ship, 75b51e2 llms.txt + Copy buttons + heartbeat. PREREG.md UNTOUCHED (sha256 lock integrity; the locked phrase `"BENE supersedes"` stays verbatim). Cross-repo: agentdex-cli 61e525e3 patched doc_lint.py to skip `site/` deploy artifacts (matches the existing docs/references/external/ verbatim-upstream carve-out pattern), then mirrored the bene-main HEAD as d0c33d58. bene-site mirror: add884c. Koyeb POST → build T+~240s → live verification: /bene/, /bene/llms.txt, /bene/SKILL.md, /bene/docs/, /bene/docs/benchmarks/COMMUNITY-BENCH-REPORT.html, /bene/docs/research/GAP-AUDIT.html, /bene/docs/design/CLAIMS-AUDIT.html, /bene/docs/architecture.html ALL 200; /bene/docs/benchmarks/RIVAL-BENCH-REPORT.html correctly 404 (rename complete); /ladder, /enrollment gateway substance 200. Landing HTML smoke-check: 1 "Docs ↗" pill, 1 hero CTA, 2 CopyLlmsTxtButton refs, 1 COMMUNITY-BENCH-REPORT link, ZERO rival mentions in served HTML, ZERO "pillars / three surfaces". llms.txt + SKILL.md heartbeat heading both present in served content. Gates: pytest 736/6/0 (env drift, invariant 0 failed unchanged), ruff check clean. Queued next: user-requested zh i18n version of landing + 42 doc pages + i18n toggle on every page (substrate-level — landing was English-only since the 2026-06-12 Patagon redesign; build-docs.py needs an i18n-aware template path).
- 2026-06-16 — Due-diligence audit + fix-all (bene-8 session, ultracode + feedback-fix-all policy). User: "due diligent research on bene's features — is it truly like the content claims, does bene solve the problems popular agentic frameworks haven't caught up on." Ran a 71-agent workflow (wf_fcf04f0e-715, ~4M tokens): every landing claim verified against live code EMPIRICALLY (ran the CLI + each recipe verify-command) then adversarially refuted, PLUS per-framework web research on the competitive claim. Report committed at `.supergoal/DUE-DILIGENCE-2026-06-16.md` (ea65ec3). **Verdict: engineering is real (sha256 kill gate, 5-tier engrams, mandatory provenance, computed 4-signal trust, human-only L4, 37 MCP tools, per-agent VFS isolation, intent→vote→decide SharedLog — all verified + wired); competitive framing was overstated.** G3 (autonomy computed from audit trail) genuinely unmatched natively = strongest moat; G1-narrow (hash-locked tamper-refusing gate) unmatched; G1/G2/G4 BROAD = table-stakes (Letta gates / ADK criteria / DSPy-GEPA hard-block evals; Letta/LangMem/CrewAI/Mem0 tiered+shared memory; LangGraph SqliteSaver / OpenAI SQLiteSession SQLite state) — the real differentiator is the CONJUNCTION (5 domains in one auditable file). **4 kernel bugs filed as issues on origin good-night-oppie/bene** (bene-core's lane): #1 P0 promote() forge gap ("no back door" not enforced), #2 P1 default run emits 0 engrams, #3 P1 trust.eligible() zero-callers (no auto-promotion), #4 P2 ContextOS id() manifest bug. **10 landing-copy fixes shipped as tiny commits (8683f21→51b1358, all EN+ZH, 0 BLOCK throughout)**: FIX-1 "no back door"→"no second path"; FIX-2 engrams "every run by default"→"attach the kernel"; FIX-3 deleted "tiers auto-promote"→"advisory, grants explicit"; FIX-4 integration "recipes"→"patterns you wire yourself (no adapter package)"; FIX-5 "No YAML knob"→"no flag bypasses the human grant"; FIX-6 VOID="inadmissible gate"; FIX-7 tamper→"refuses to run (LockTamperError)"; FIX-8+9 competitive h2→"your stack closes some — BENE closes them in one auditable file" (union); FIX-10 broken recipe commands (R2 log tail / R5 ContextOS().assemble(budget_tokens) / R6 bene trust <id> / R7 mh search --benchmark). 3-mirror sync (bene-site 6ac030f, agentdex-cli 23824536) + Koyeb deploy via ~/.ai-builders.json key (op bypassed) → live-verified all 10 fixes present + all 8 false claims 0× on BOTH EN + ZH; gateway 200. bene-core-2 pushed a complementary SKILL.md honesty sweep concurrently (c8cd679/3c28762, ff, no conflict). Linter 0 BLOCK / 29 WARN; 68 linter tests pass. **Clean state reached — full diligence queue worked top-to-bottom.**
- 2026-06-16 — Kernel bug fixes in worktrees (bene-8 session, user: "work on worktrees to resolve above problems"). bene-core confirmed on agentdex-cli hardening, so bene-main kernel was mine. Fixed the 2 genuine BUGS (the other 2 issues are design/feature decisions already reconciled by the FIX-2/FIX-3 copy edits, deliberately NOT built unilaterally — flagged on issues #2/#3 as product/architecture decisions): **#1 P0 promote() forge gap → CLOSED** (`fix/promote-lock-enforcement`): promote() now requires the ACCEPT verdict be probe-backed — an `experiment_runs` row → an *admissible*, lock-intact `probe_registry` entry (new helper `_verdict_is_probe_backed`); a hand-appended eval engram via store.append+link no longer promotes. +2 tests (forged-accept, inadmissible-probe-accept). **#4 P2 ContextOS manifest → CLOSED** (`fix/contextos-manifest-identity`): `assemble()` stamped a stable `_uid` before packing instead of `id()` (which broke under compress-then-pack's `{**i}` rebuild → every original reported as both included+dropped); tag stripped from `included`. +1 partition test. Both: full suite 930→931 passed / 0 failed, ruff clean, per-fix worktree → commit → ff-merge → push → worktree removed. **Concurrency note: good-night-oppie/bene is now actively MULTI-WRITER** — sibling A2A-feature commits (7603985 A2A-on-SharedLog, b77377c) landed on origin mid-session; my contextos ff-merge failed (diverged) and I recovered via cherry-pick onto the advanced main. Final origin HEAD 25780ee carries both fixes (verified: `_verdict_is_probe_backed` ×2 in gepa.py, `_uid` ×5 in contextos.py). Issues #1/#4 CLOSED; #2/#3 OPEN with status comments (copy already honest; feature build = product decision).
- 2026-06-15 — Linter ZH undercoverage closed + slop paragraph killed (bene-8 session, harness-6 cross-lineage receipt). After the linter shipped at '0 BLOCK / 51 WARN', user pulled site/zh/index.html:867 — "Dune 比喻放在...这个顺序才稳。每一个 Bene Gesserit 的 beat，都能落到一个真的 CLI verb" — and called it slop. harness-6 dispatched the exact regex extensions needed: BENE-LINT-002/004/009/019 had English-only patterns and missed the ZH cousin of "every beat maps to a verb" silently. **Per spec, no 21st rule** — extended the four existing regexes: (002) added non-html-anchored ZH alt `每(一)?个 X 都能?(落到|对应|映射|落实) Y` where Y ∈ {CLI, verb, 命令, API, primitive, 动词, 动作}; (004) DROPPED `\b` from CJK literals (proven empirically: Python `\b` never fires between two word-chars under unicode, so existing 看板/基座/接管子 were latent misses in flowing ZH — bene-7's CJK literals only fired when surrounded by ASCII spaces) AND added 顺序才稳 / 这才稳 / 才出场; (009) extended section-lead defense within first 200 chars of `<p>` with ZH triggers (这个顺序才稳, 放在 X 后面才出场, 留到最后才, 顺序更稳, 先后顺序); (019) added 都能?(落到|映射到|映射成|对应到) 一个. Re-run linter on bene-main HEAD pre-fix: 4 BLOCKs on site/zh/index.html L865-867 — all 4 extended rules fire correctly. **Fixtures + regression walker**: created tests/fixtures/landing_copy_linter/<rule_id>/{should_block,should_pass}/ dir layout per harness-6 spec — verbatim L867 paragraph in each should_block/zh-line-867.html (4 files), concrete-scene replacement (`凌晨三点你 ssh 进去，敲 bene memory search ...`) in each should_pass/zh-replacement.html (4 files). Added a dir-walker pytest (test_fixture_should_block + test_fixture_should_pass) that parametrizes over every file; dropping a new fixture in the right dir is the only step to lock a future regression. Total tests 61 (was 53). **The fix**: Option A per spec — DELETE the offending `<p>` entirely on both site/zh/index.html L866-868 AND the EN twin at site/index.html L877-879 ("The Dune metaphor earns its place after the demo, the comparison, and the limits. Each Bene Gesserit beat is paired with a real CLI verb"). H2 + badge grid + Litany blockquote answer the origin question on their own. Litany allowlist entries shifted L897→L894 (EN) + L886→L883 (ZH) after the 5-line delete; allowlist justification field updated. **Final linter state**: 0 BLOCK / 51 WARN (same shape as bene-8's prior baseline but now WITH ZH coverage on the slop pattern). 61/61 tests pass. Pytest core suite 736/6/0 unchanged. **Commits**: bene-main `5b6c200` (paragraph delete) + the rule/fixture/walker scaffold commit just before it (regex broadening). bene-site mirror `837b232`, agentdex-cli `5e96cfd1` (origin + deploy-target). Koyeb POST → live in T+50s (warmer cache than first deploy). Live verify: all 6 receipt strings 0× on EN + ZH live HTML, H2/Litany/badge-grid all still rendering, 3-mirror md5 invariant intact, gateway substance unchanged (/, /ladder, /bene/, /bene/zh/, /bene/SKILL.md, /bene/llms.txt, /bene/docs/README.html all 200). **Lesson**: the bene-8 "0 BLOCK / 51 WARN" green was exactly the failure mode the audit warned about — silent ZH undercoverage. ASCII-only regex on a bilingual surface is a structural miss, not a content miss; the `\b` CJK gotcha makes it worse because the regex LOOKS bilingual but never fires.
- 2026-06-15 — Landing-copy linter shipped + live recidivism cleanup (bene-8 session, picks up bene-7's stalled linter scaffold + first baseline triage). Predecessor bene-7 landed `scripts/landing_copy_linter/{__init__.py,kill_list.py (14 entries),rules.py (20 rules),allowlist.yaml (empty seed)}` and the 377-line entrypoint `scripts/landing_copy_linter.py` but stood down before tests/CI wiring/baseline triage. **2 pre-existing latent bugs in the scaffold caught + fixed**: (1) `rules.py` BENE-LINT-010's regex had mid-alternation `(?m)` / `(?mi)` flag groups — illegal in Python 3.6+; linter failed to import. Moved flags to start-of-pattern + scoped IGNORECASE to one alternation via inline `(?i:...)`. (2) Entrypoint's `matches_any` used `fnmatch.fnmatch` which silently dropped direct-child matches like `site/docs/foo.html` vs `site/docs/**/*.html` (fnmatch translates `**` to `.*` requiring an intervening `/`). Replaced with a 23-line `_glob_to_regex` helper that handles gitignore-style `**/`, `/**`, `**`, `*`, `?` correctly; lru_cached + 12 test cases. **Baseline triage**: 547 violations / 112 BLOCK first cut; narrowed `DEFAULT_GLOBS` from `docs/**/*.md + site/**/*` to the actual landing surface (site/{index.html,zh/index.html,SKILL.md,llms.txt}, README.md — internal design/research docs intentionally use category nouns like 'substrate', so policing them is out of scope for a *landing*-copy linter). Down to 91 violations / 32 BLOCK. 30 of the 32 BLOCKs were genuine recidivism on the live landing — including the canonical 'trust is computed, not declared' kill from the 2026-06-14 codex pass that had been reintroduced in 3 separate places (site/index.html L406 + L619 + SKILL.md L57) and 'BENE is a runtime ...' back on README.md L39. Fixed all of these as verb-shape rewrites per the kill rationale; 6 genuine FPs (Litany verbatim quotes ×3, single-file-SPA BENE-LINT-005 structural mismatch ×2, 'Everything is an engram' kernel-design mantra ×1) allowlisted with mandatory justification + expires 2030-01-01. **Tests**: 53 new tests in `tests/test_landing_copy_linter.py` — parameterized per-rule should_block/should_pass fixtures (36 cases), gitignore-glob helper table, allowlist silencing + expiry + KILL_LIST short-circuit semantics, end-to-end CLI on tmp files (exit-code, JSON output, LANDING_COPY_LINTER_TODAY envvar-driven date). **CI wiring**: appended to `scripts/lint` umbrella + new stage in `.github/workflows/reusable-fast-gate.yml` + new `.pre-commit-config.yaml` with a local landing-copy-linter hook filtered to the landing surface (the hook only fires when actual landing files OR the linter package itself changes). **Mirror chain + deploy**: 2 commits on bene-main (`c44616d` content fixes + `92f477e` linter scaffold) → /tmp/bene-site mirror (`b7df2a6` after a fix-up reverting an incorrect README.md mirror — bene-site/README.md is a 7-line repo identifier for the GitHub mirror, NOT a sync target from bene-main; documented as a future-agent trap in the `bene-site-deploy-pipeline` memory) → agentdex-cli (`1318f80c`) → push origin + deploy-target. Koyeb POST via `$SPAC_AI_BUILDERS_API_KEY` env-var bypass (op rate-limit per 2026-06-15 memory) — first try HTTP 422 on wrong payload schema, second try with correct `repo_url + service_name=agentdex + branch=main + port=8000` returned HTTP 202 → live in T+140s (small diff cold build). **Live verify all green**: 8 verb-form rewrites present (1× each), 6 kill-list recidivism strings 0× hits on live HTML, SKILL.md verb-form 1× / old phrase 0×, llms.txt verb-form 1× / old phrase 0×, Litany verbatim 1× on EN and 1× on ZH, gateway substance unchanged (/, /ladder, /bene/, /bene/zh/, /bene/SKILL.md, /bene/llms.txt, /bene/docs/README.html all 200), 3-mirror md5 sync invariant intact. Pytest 736/6/0 unchanged (the +53 linter tests are scoped to the linter and not part of bene's core regression invariant — they live in tests/test_landing_copy_linter.py and run via uv run pytest tests/ -q like everything else). Predecessor's full 4-step plan complete: linter core ◼ → tests/fixtures ◼ → pre-commit + CI wiring ◼ → baseline + triage ◼.
- 2026-06-13 — Round-4 convergence audit (wf_8664231b-32d, 5 dims × adversarial verify): 4 dims CLEAN — `commit-message-claims-vs-code` (all 11 session commit messages match landed code), `live-render-deep` (each of /bene/, /bene/docs/, /bene/docs/cli-reference, /bene/docs/architecture, /bene/docs/mcp-integration, /bene/docs/use-cases live with correct content + onerror guards), `bene-site-spa-staleness` (bene-site site/index.html matches canonical byte-identically), `state-md-vs-git-history` (every cited commit hash resolves; no STATE.md claim contradicted by git log). 1 confirmed gap (medium): a REAL footgun the round-3 Dockerfile strip didn't cover — `build-docs.py` was scrubbed from the deploy image but stayed on the agentdex-cli working-copy disk. The script uses `ROOT = Path(__file__).resolve().parent.parent  # bene-main` — a hardcoded comment, not a runtime check. Running `uv run … python site/build-docs.py` from `agentdex-cli/` (natural "I am at site/ root" assumption) made ROOT resolve to agentdex-cli, DOCS to agentdex-cli/docs/ (2 files, not the canonical 80+), and silently clobbered the committed agentdex-cli/site/docs/ deploy artifact with 12 wrong-source pages. Audit reproduced; orchestrator independently reproduced again on the first sentinel-test (and survived via `git checkout HEAD -- site/docs/`). Two-part defense shipped: (1) bene-main commit `1718297` adds `if not (ROOT/'bene').is_dir(): sys.exit(...)` at the top of build-docs.py — wrong-tree invocations now fail fast with a clear message instead of corrupting the deploy tree. Verified: still "built 37 pages" from bene-main; fails-fast in agentdex-cli with the diagnostic. (2) agentdex-cli commit `ad41bfe3` `git rm`s the 3 dev files (build-docs.py / check-i18n-parity.mjs / test-harness.html) from on-disk AND adds them to `.gitignore` under a documented "BENE landing dev-tooling" section, so future bulk-syncs (`cp -r bene-main/site/ → agentdex-cli/site/`) leave them untracked instead of accidentally re-tracking via `git add site/`. Live deploy unchanged (Dockerfile already stripped them from the image — no Koyeb redeploy needed). Round-4 also surfaced that agentdex-cli upstream had advanced (PR bots merging arena work during the audit) — handled fine via fast-forward.
- 2026-06-12 — Critic loop rounds 1-2 + magicpatterns docs (bene-4 session, commits 4428288 + 54bccdd): Round-1 (wf_dd9cd643): both lenses "mostly satisfied"; 13 consolidated fixes applied (PG: H1 'trust'→'replay', for-who line, trust-line promoted, vendor stats→reader-felt outcomes, headline de-cuting; grapeot: 01.5 Context-Engineering section, 04.5 three starter recipes, capability-delta bullets, contract-stays clarification, footer community hook). Round-2 (wf_108f24ac): critics RE-VERIFIED claims against live repo + CLI and caught 4 execution failures (hero CTA dies on public repo — no package; Recipe 1 used Python-API-only verbs as CLI + bash bug; Recipe 2 used nonexistent probe create/run; Discussions link 404). All fixed and EXECUTED before publishing (Recipe 1 ran end-to-end; probe show resolves by NAME not id — copy corrected; Discussions enabled via gh repo edit; probe-authoring CLI added to roadmap as Planned). Round-3 sign-off in flight (wf_0aea9192). Parallel: magicpatterns docs site (editor nr7va6yznlfpu7vc2jb3qk, preview project-precious-celery-515.magicpatterns.app) — 7 doc pages, Shadcn, 3 VHS demos embedded; fixed its hallucinated repo URL/paths/CLI semantics + a React #130 blank-page (artifact clone had EMPTY Shadcn primitive files — platform injects design-system code at original compile only; wrote real Card/Separator/Badge/Button/Table and republished; all routes render). Light theme on user request (warm off-white + ink + amber; terminals stay dark). Headless render-verify pipeline added: site/test-harness.html + chromium --headless (browser-use MCP can wedge).
- 2026-06-12 — Landing page rebuilt benefit-first (apple-wwdc2026-keynote skill, bene-4 session, commit fc670cf): user critique was the landing was "too self-immersive" and brought "not much attention or value to users/customers." Researched WWDC 2026 AI-dev sessions in a fan-out + adversarial-verify workflow (15 agents, 856k tokens) — sourced + cited only what loads from developer.apple.com / Apple Newsroom / MacRumors / TechCrunch / TechTimes / 9to5Mac / Six Colors / AppleInsider; nothing fabricated. Distilled into a new skill `apple-wwdc2026-keynote` (12 cited principles incl. benefit-first, demo-as-proof, plain naming, subtraction, verb-shaped trust line; full WWDC26 corpus at ~/.claude/skills/apple-wwdc2026-keynote/references/). Applied to landing: H1 dropped Breeding-program backronym → 'Run AI coding agents you can rewind, audit, and trust'; 5 jobs-to-be-done sections (Watch it run / Rewind a bad turn / Receipts / Drop into tools / Today vs Soon) replacing 8 lore sections; 3 real VHS terminal demos (demo-keyless 0.3s · demo-litany checkpoint→diff→restore · demo-honesty experiments+senses) recorded with VHS v0.11.0 against bene-main HEAD, no edits; Dune/Litany demoted to a labeled "Why the name" section at page foot. Light theme: warm off-white canvas + deep ink + one warm amber accent + dark terminals. en/zh parity green (173 nodes). Deployed (Koyeb rebuild ~3 min); live live-render-verified on the warm-light theme. Magicpatterns design (Shadcn, editor nr7va6yznlfpu7vc2jb3qk) generating in parallel for a dedicated multi-page docs site embedding the 3 demos. Phase 5 critic loop (grapeot + paul-graham, fan-out + synthesize residuals) running as wf_dd9cd643-9f5.

---

## ZH_REWRITE_V2_DEPLOYED ts=2026-06-13T23:55:21Z (POST) / T+30s (live verified)

User confirmed "1. yes 2. yes" → deploy push authorised. Executed sanity → sync × 3 → Koyeb POST → live verification → STAND DOWN per instruction.

**Pre-deploy sanity gates:**
- ruff: clean (after `77a9314` fixed F401+F541 in `.supergoal/research-lenny/render_distillations.py`; a side-effect commit `6aae5fd` accidentally emptied the file via linter behaviour between my Read and add and was reverted in `e51e428` before the proper fix landed).
- i18n parity: 203 nodes identical.
- pytest: **736 passed, 6 skipped, 0 failed** (drift from 741/1/0 is purely env: 5 postgres-backend tests now skip with ConnectionRefusedError; 1 tier-router test skips on missing OPENAI_API_KEY. CLAUDE.md note 29 documents this drift pattern. Invariant (0 failed) holds; my v2 commits don't touch tests/.)

**Sync chain (3 git mirrors):**
- `EdwardTang/bene-site` (GitHub mirror linked from Magic Patterns docs): commit `642f36f`, pushed.
- `good-night-oppie/agentdex-cli` (origin): commit `004063fd` (rebased onto PR #100 ADR-0011 that landed during this session), pushed.
- `EdwardTang/agentdex-arena` (deploy-target, Koyeb watch): commit `151b8662` (pre-rebase SHA, byte-identical content), pushed. **Mirror SHA-lag flag**: `151b8662` is not on agentdex-cli/main (replaced by `004063fd` post-rebase); content identical, not deploy-blocking, future agents should align via `git push deploy-target main` from agentdex-cli when convenient.

**Koyeb deploy:**
- POST → `space.ai-builders.com/backend/v1/deployments` with 1P key (op read succeeded first try).
- Build → live in ~30s.
- Live verification: all 14 v2 markers present in `https://agentdex.ai-builders.space/bene/index.html` (59 414 bytes); sentinel `一个 agent = 一份 SQLite 文件` count = 0 (rejected句式 fully scrubbed from live); gateway substance intact (200 on /, /ladder, /enrollment, /bene/docs/README.html).

STAND DOWN per user instruction "ship 完直接 STAND DOWN, 别开新工作".

---

## zh-rewrite-v2 SHIPPED (2026-06-13, user-requested overlay)

After STEP 2 PAUSE, user confirmed 4-book shortlist: **The Mom Test (Fitzpatrick)** + the 3 books from Lenny's "improve my communication skills" bucket (**Nobody Wants to Read Your Sh\*t** — Pressfield; **On Writing Well** — Zinsser; **Storyworthy** — Dicks). Workflow `wf_3585b934-c3c` spawned 4 parallel distillation subagents (commit `b4def22`); 14 zh rewrites across 6 tiny commits + 1 merge commit on top of the rejected v1 chain.

Commits (all `[user-requested-rewrite-v2]`-tagged for grep):
- `a41b355` hero (title / forWho / trustLine / tagline) — 4 rewrites
- `9769366` watch (title / body)
- `6c1c878` builders (title / body) — Pressfield 7Up-the-Uncola concept-first reframe; THE primary X=Y offender killed
- `98e77db` rewind (title / body) — Pressfield Hook→Build→Payoff
- `ddb4ab5` receipts (title / body) — Pressfield stakes-on-the-table
- `89f8de0` build (title / body) — kill Fitzpatrick "compliment that costs nothing"
- `bceeabd` merge --no-ff (rich audit trail in commit body)

Convergence — all 4 books independently reject the same v1 X=Y句式 ('一个 agent = 一份 SQLite 文件') for distinct reasons: Mom Test (idea-first → consultant), Pressfield (Client's Disease), Zinsser (concept-noun with no people / no working verbs), Dicks (anecdote-as-definition / no moment of change). This convergence is the evidence base that makes the rewrite authoritative rather than another taste-based intuition.

Gates: i18n parity 203/203 after every commit; EN hydrate 272KB DOM (no white-page); pytest 741/1/0 unchanged. Live deploy NOT yet triggered (the bene-main → agentdex-cli/site/ sync + Koyeb POST sequence is separate; can be triggered when the user wants it live).

Evidence base on disk: `docs/research/product-comms/{mom-test, nobody-wants-to-read, on-writing-well, storyworthy}.md` (4×~14KB). KAOS: memory_ids 5-8 at keys `research/product-comms/<slug>` (metadata.role='official_v2', supersedes_memory_id={1-4} stubs created accidentally during FK-satisfying spawn).

---

## Book shortlist (PAUSE for user — 2026-06-13, post-zh-rewrite rejection)

**Source**: Lenny Rachitsky, *Essential books for product builders — part 1* (9 buckets / 27 books accessible; last 2 buckets paywalled — full text saved at `.supergoal/research-lenny/source.md`).

**User-specified scope**: "(a) Communication / writing / framing — Crucial Conversations / Made to Stick / Trust me I'm Lying caliber" + "(b) Product success rate / customer discovery / why-products-fail — 4 Steps to the Epiphany / Inspired / Continuous Discovery Habits caliber". Exclude leadership / hiring / general management.

**Awkward fact to surface before commitment**: of the books you named as caliber examples, **only 2 are in Lenny's Part 1** (Inspired, Continuous Discovery Habits). *Crucial Conversations*, *Made to Stick*, *Trust me I'm Lying*, and *4 Steps to the Epiphany* are **NOT in Part 1**. So the choices are:

- **Option A — Stick strictly to Lenny's Part 1** (cleanest, single source-of-truth, but loses the named-caliber titles)
- **Option B — Lenny's Part 1 + supplement with the named-but-absent titles** (broader evidence base, but I'd be reading 8+ books over multiple distillation passes and the extra titles aren't from the curated source you anchored on)
- **Option C — Wait for Lenny Part 2** (URL: `lennysnewsletter.com/p/essential-books-for-product-builderspart-611` — confirmed exists via search; might cover Crucial Conversations / Made to Stick / etc.)

### Proposed shortlist (Option A — 7 books)

**(a) Communication / writing / framing — 4 books**

| # | Book | Author | Lenny's bucket | Why it's load-bearing for the BENE rewrite |
|---|---|---|---|---|
| 1 | *Nobody Wants to Read Your Sh\*t* | Steven Pressfield | "improve my communication skills" | Direct hit on your diagnosis. Pressfield's whole thesis is "your reader is busy, distracted, and doesn't care about you — write accordingly." This is the exact `git stash 救不回来` move I should have built every line around. |
| 2 | *On Writing Well* | William Zinsser | "improve my communication skills" | The canonical "remove every word that doesn't earn its place" book. Antidote to叠词 + Western翻译腔 + 抽象比喻 — the three failure modes in my last zh draft. |
| 3 | *Storyworthy* | Matthew Dicks | "improve my communication skills" | Stakes-based story arc. The "agent 改炸生产" line is reaching for this but underbaked — Dicks's framework on "5-second moments" is exactly the missing technique. |
| 4 | *Obviously Awesome* | April Dunford | "sales and marketing" | **Positioning** — which is what landing copy *is*. Dunford's frame ("competitive alternatives → unique attributes → value → who-it's-for") is a direct replacement for `X = SQLite` reductive句式. Lenny's bucket is "sales and marketing" but the book is really about how to *frame what your product is* to a reader, so it fits scope (a) by function even though Lenny shelves it under (b). Flagging the bucket-mismatch so you can veto if you want strict strict. |

**(b) Product success rate / customer discovery — 3 books**

| # | Book | Author | Lenny's bucket | Why it's load-bearing for the BENE rewrite |
|---|---|---|---|---|
| 5 | *The Mom Test* | Rob Fitzpatrick | "increase my product success rate" | How to talk to customers without leading them. Will surface the actual pain language a real BENE user uses — that's the language the landing should mirror, not vendor-jargon. |
| 6 | *Continuous Discovery Habits* | Teresa Torres | "increase my product success rate" | You named this directly. The "opportunity solution tree" gives a concrete shape for "describe a scenario the reader recognizes → what BENE does in it" — the exact missing mental model from my last draft. |
| 7 | *Inspired* | Marty Cagan | "level up my product org" (note: bucket is org, not success rate — flagging) | You named this directly. *Inspired* is more about how product orgs work than direct copy guidance; less directly actionable for landing rewrite. Optional — drop if you want a leaner shortlist. |

**Excluded but considered (for transparency)**

- *Escaping the Build Trap* (Perri, bucket: success rate) — outcome-vs-output thinking is relevant but heavily overlaps with *Continuous Discovery Habits*. Excluded to avoid distillation redundancy.
- *Good Strategy/Bad Strategy* (Rumelt, bucket: strategy) — Rumelt's "diagnosis → guiding policy → coherent action" framing is genuinely relevant to landing copy ("what's the diagnosis your reader has?"). Excluded because strategy bucket is outside your stated scope, but if you want I can pull it in.
- *Purple Cow* (Godin, bucket: sales/marketing) — "remarkable enough to be worth telling" overlaps thematically with Pressfield + Dunford. Excluded for redundancy.

### Decisions you need to make

1. **Option A / B / C above?**
2. **Drop book #7 (*Inspired*) to get to 6 books?** Or keep 7?
3. **Add *Made to Stick* (Heath & Heath) from outside Lenny's list?** It's the canonical "make ideas concrete + credible + emotional + story" book and would fix the "reader's brain model" gap directly — but it's not in this Part 1, so adding it breaks the "source = Lenny" constraint.

Once you confirm shortlist + answers above, STEP 3 spawns one subagent per book to distill into `docs/research/product-comms/<book-slug>.md` + KAOS memory. **PAUSING here.**
