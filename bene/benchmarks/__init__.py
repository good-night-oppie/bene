"""Top-level bene benchmarks namespace.

Distinct from `bene.metaharness.benchmarks` (which contains the abstract
`Benchmark` base class and the originally-bundled reference benchmarks like
agentic_coding, arc_agi3, etc.) — this namespace is for higher-level
domain-specific benchmark packages, each shipping its own seed
harnesses, calibrated data snapshot, and runners.
"""
